# 升级到 3.0.1

## 从 3.0.0 升级

本次仅更新本地报告图片样式，不更改 API、个人绑定、推送订阅或配置结构。停用插件并备份后，通过 ZIP 更新源码再重载即可；保留原插件配置与数据目录。发送「光遇更新」确认 3.0.1，开启 `report_images` 后测试身高或其他文字报告。无需安装 Chromium。

以下保留 3.0.0 大版本的迁移说明。

本版本以仓库2.5.3为发布基线，采用模块化Python实现并加入本轮测试的独角兽接口、token自助绑定、群订阅和提前提醒。插件标识仍是 astrbot_plugin_sky。

## 从仓库2.5.3升级

1. 停用插件，备份整个旧插件数据目录、sky_bindings目录及 data/config/astrbot_plugin_sky_config.json。
2. 更新源码，打开插件配置，按新版字段重新填写自己的密钥。旧 sky_api_key 对应新的 t1qq_api_key；其他密钥不能盲目通用，应确认供应商及接口权限。旧配置名不会自动迁移。
3. 旧JSON绑定文件保留，不删除，但不会自动导入SQLite；用户需用新版命令重新绑定。旧群号推送列表不会自动开启新订阅，请在各群发送开启命令。
4. 当前正式支持 OneBot11/NapCat；2.5.3标注的企业微信和Telegram不在本次实现的支持范围。
5. 本地手动蜡烛记账已移除；季节进度现在提供API季节结束时间。命令与LLM工具有较大调整，请以README为准。

## 从本对话提供的 astrbot_plugin_tlon_sky 安装包切换

先备份，停用原插件，避免两份插件同时响应。同一服务器上的 data/plugin_data/astrbot_plugin_tlon_sky/sky.sqlite3 继续使用，因此原本的token、群订阅等保留。配置文件标识换成 astrbot_plugin_sky，请在新版后台复制原配置项，确认成功后再移除旧插件源码；不要删除共享数据目录。

为兼容这些已安装用户，3.0.0仍明确使用 astrbot_plugin_tlon_sky 数据目录。这与仓库/插件标识不同，是有意保留，不应直接重命名。

## 验证与恢复

先发送「光遇更新」确认3.0.0，再测试菜单、一个查询与推送状态。已保存token不代表远端授权有效。52项本地回归通过，但未验证真实API额度、QQ发送与生产升级。

回退时先停用3.0.0，恢复备份源码与配置。新SQLite数据与旧JSON分开，旧JSON未被覆盖；不要在启用状态下复制运行中的SQLite文件。
